package net.minesky.api.connection;

import net.minesky.core.CoreMain;
import net.minesky.spigot.misc.SpigotConnection;
import net.minesky.velocity.misc.VelocityConnection;

public class PlayerConnection {

    /*public static void sendPlayerToServer(String playerName, String server) {
        if(CoreMain.currentPlatform == CoreMain.PlatformType.VELOCITY)
            VelocityConnection.sendPlayerToServer(playerName, server);
        else
            SpigotConnection.sendPlayerToServer(playerName, server);
    }*/

}
